// src/add.zig
/// Adds two integers and returns the result.
///
/// # Parameters
/// - `a`: The first integer.
/// - `b`: The second integer.
///
/// # Returns
/// The sum of `a` and `b`.
export fn add(a: i32, b: i32) i32 {
    return a + b;
}
